name: Daily Cleanup

on:
  schedule:
    # Run at 2:30 AM UTC Monday to Friday (8:00 AM IST)
    - cron: '30 2 * * 1-5'
  workflow_dispatch: # Allow manual trigger for testing

jobs:
  delete-analysis-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Debug Secrets
        run: |
          echo "🔍 Checking secrets..."
          echo "SUPABASE_URL length: ${#SUPABASE_URL}"
          echo "SUPABASE_SERVICE_ROLE_KEY length: ${#SUPABASE_SERVICE_ROLE_KEY}"
          if [ -z "$SUPABASE_URL" ]; then
            echo "❌ SUPABASE_URL is empty!"
            exit 1
          fi
          if [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "❌ SUPABASE_SERVICE_ROLE_KEY is empty!"
            exit 1
          fi
          echo "✅ Secrets are loaded"
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Delete Option Chain Analysis Data
        run: |
          echo "🗑️ Deleting all data from option_chain_analysis table..."
          
          curl -X DELETE \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/option_chain_analysis?id=gte.0"
          
          echo "✅ Option chain analysis table cleared"

      - name: Delete Futures Analysis Data
        run: |
          echo "🗑️ Deleting all data from futures_analysis table..."
          
          curl -X DELETE \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/futures_analysis?id=gte.0"
          
          echo "✅ Futures analysis table cleared"

      - name: Delete Most Active Stock Calls Data
        run: |
          echo "🗑️ Deleting all data from most_active_stock_calls table..."
          
          curl -X DELETE \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/most_active_stock_calls?id=gte.0"
          
          echo "✅ Most active stock calls table cleared"

      - name: Delete Most Active Stock Puts Data
        run: |
          echo "🗑️ Deleting all data from most_active_stock_puts table..."
          
          curl -X DELETE \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/most_active_stock_puts?id=gte.0"
          
          echo "✅ Most active stock puts table cleared"

      - name: Verify Cleanup
        run: |
          echo "🔍 Verifying tables are empty..."
          
          # Check option analysis count
          OPTION_COUNT=$(curl -s \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/option_chain_analysis?select=id" \
            | jq '. | length')
          
          # Check futures analysis count  
          FUTURES_COUNT=$(curl -s \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/futures_analysis?select=id" \
            | jq '. | length')
          
          # Check most active stock calls count
          CALLS_COUNT=$(curl -s \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/most_active_stock_calls?select=id" \
            | jq '. | length')
          
          # Check most active stock puts count
          PUTS_COUNT=$(curl -s \
            -H "apikey: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/most_active_stock_puts?select=id" \
            | jq '. | length')
          
          echo "📊 Final Count:"
          echo "   option_chain_analysis: $OPTION_COUNT records"
          echo "   futures_analysis: $FUTURES_COUNT records"
          echo "   most_active_stock_calls: $CALLS_COUNT records"
          echo "   most_active_stock_puts: $PUTS_COUNT records"
          
          if [ "$OPTION_COUNT" -eq 0 ] && [ "$FUTURES_COUNT" -eq 0 ] && [ "$CALLS_COUNT" -eq 0 ] && [ "$PUTS_COUNT" -eq 0 ]; then
            echo "✅ SUCCESS: All tables are completely empty!"
          else
            echo "⚠️ WARNING: Some tables still contain data"
            exit 1
          fi
